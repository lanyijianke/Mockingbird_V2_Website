# Agent Assets Data Readiness Report

Date: 2026-06-08

## Gate Summary

Phase 2 vector indexing may start after this report is reviewed.

The Phase 1 code surface is healthy, source-vs-index coverage is complete, and all active prompts plus published articles are represented in `AgentSearchDocuments`. The original all-assets HTTP reindex path was too coarse for interactive use, so a batch-safe prompt reindex path was added and used to finish coverage.

## Verification

Focused tests passed:

```bash
npm test -- \
  tests/unit/agent-asset-normalizer.test.ts \
  tests/unit/agent-search-assets.test.ts \
  tests/unit/agent-search-api.test.ts \
  tests/unit/agent-search-indexer.test.ts \
  tests/unit/agent-assets-audit.test.ts \
  tests/unit/mockingbird-knowledge-skill.test.ts
```

Result:

```text
Test Files  6 passed (6)
Tests       20 passed (20)
```

## Reindex Summary

Full reindex was attempted through:

```bash
POST /api/agent/index
{"type":"all","site":"ai"}
```

Observed behavior:

- The first attempt failed before indexing because the shell-loaded admin token was empty.
- The second attempt authenticated and entered the route, but the client request remained open for several minutes without a response.
- The Next.js server stayed healthy and continued serving other Agent search requests.
- A single prompt reindex completed successfully.
- A single article reindex completed successfully.

Single-item verification:

```text
prompt:7659 indexed
article:twitter-2062720923942228205 indexed
```

Current conclusion:

- The indexer path works for individual assets.
- The original all-assets HTTP route is too slow or too coarse for the current real prompt volume as an interactive request.
- A bounded `prompt-batch` index request now supports cursor-based prompt indexing.
- `scripts/agent-prompt-batch-reindex.mjs` was used to process all active prompts in observable batches.

Batch prompt reindex result:

```text
Batches: 39
Indexed prompts: 7659
Skipped prompts: 0
Failed prompts: 0
Final cursor: 7659
```

## Source Vs Index Coverage

Generated by:

```bash
node scripts/agent-source-index-coverage.mjs --site=ai --format=markdown
```

| Metric | Value |
| --- | ---: |
| Site | ai |
| Active prompts | 7659 |
| Indexed prompt documents | 7659 |
| Prompt gap | 0 |
| Published articles | 17 |
| Indexed article documents | 17 |
| Article gap | 0 |
| Total indexed documents | 7676 |
| Total chunks | 9181 |

Status:

- Prompt coverage: covered
- Article coverage: covered

## Asset Audit Findings

Generated by:

```bash
node scripts/agent-assets-audit.mjs --site=ai --format=markdown
```

Important limitation:

- This audit reads `AgentSearchDocuments`, so it audits only already indexed assets.
- It does not prove source coverage.
- The source-vs-index check above is the authoritative coverage gate.

Indexed asset audit:

| Metric | Value |
| --- | ---: |
| Total indexed audited assets | 7676 |
| Indexed prompts audited | 7659 |
| Indexed articles audited | 17 |
| With cover | 7674 |
| With image | 7674 |
| With video | 369 |
| With examples | 1536 |

### Critical

- None.

### High

- The old all-assets request shape is still too coarse for interactive operation. Use `prompt-batch` or `scripts/agent-prompt-batch-reindex.mjs` for large prompt reindexes.

### Medium

- Two indexed articles have no media according to the indexed asset metadata:
  - `article:other-https-claude-com-blog-best-practices-for-getting-started-with-claude-cowork`
  - `article:other-telegram-ai-news-cn-37455`
- Only 1536 indexed assets currently report examples.
- Only 369 indexed assets currently report video.

### Low

- No missing indexed descriptions or summaries were found.
- No empty indexed content was found.
- No invalid indexed media JSON was found.

## Safe Automated Fixes Applied

- Added a read-only source-vs-index coverage script:
  - `scripts/agent-source-index-coverage.mjs`
- Added a batch-safe prompt reindex script:
  - `scripts/agent-prompt-batch-reindex.mjs`
- Added protected `prompt-batch` index request support.
- Verified single prompt indexing.
- Verified single article indexing.
- Reindexed all 7659 active prompts through batch requests.

No destructive data cleanup or backfill was performed.

## Manual Content Fixes

These require editorial or product judgment:

- Decide whether the two article assets without media need cover images.
- Decide whether prompts without examples should be backfilled from existing media or left as lower-quality assets.
- Decide whether video coverage is sufficient for public skill launch.

## Required Follow-Up Before Phase 2 Vector Indexing

1. Review whether the two indexed article assets without media need cover images.
2. Review whether prompt example/video coverage is acceptable for public skill launch.
3. Start Phase 2 by creating and populating the separate `mockingbird_knowledge_assets` Qdrant collection.

## Phase 2 Gate Decision

Passed.

Prompt and article source-vs-index coverage is complete. Qdrant vector indexing and hybrid semantic search may start in the separate `mockingbird_knowledge_assets` collection after the remaining editorial media questions are accepted.
